{
  "name": "spidermines",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run --prefix frontend deploy"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
